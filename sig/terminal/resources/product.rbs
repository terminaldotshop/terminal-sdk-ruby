module Terminal
  module Resources
    class Product
      def list:
        (
          ?Terminal::Models::ProductListParams | Hash[Symbol, top] params
        ) -> Terminal::Models::ProductListResponse
        | (
          request_options: Terminal::request_opts
        ) -> Terminal::Models::ProductListResponse

      def initialize: (client: Terminal::Client) -> void
    end
  end
end
